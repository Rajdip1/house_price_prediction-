# Requirements for running the Flask API and for notebook/training convenience
# Pin minimal versions to avoid common unpickling/runtime mismatches.
Flask>=2.0
numpy>=1.21
scikit-learn>=1.0
pandas>=1.3
joblib>=1.1
gunicorn>=20.1
# Optional: add jupyter if you plan to run the notebook interactively
jupyter>=1.0
